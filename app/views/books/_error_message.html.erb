<% if message.errors.any? %>
  <h3><%= pluralize(message.errors.count, "error") %> prohibited this obj from being saved:</h3>
  <ul>
    <% message.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>